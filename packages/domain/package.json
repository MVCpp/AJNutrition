{
  "name": "@ajnutrition/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure domain model: entities, value objects, invariants. Must not import Electron, React, SQLite, fs, or network APIs.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ajnutrition/shared": "workspace:*"
  }
}
