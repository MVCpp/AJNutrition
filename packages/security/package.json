{
  "name": "@ajnutrition/security",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Key management: KDF, authenticated encryption envelopes, keyfile (passphrase + recovery slots), unlock throttling. Uses node:crypto and node:fs only — no Electron, no UI.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ajnutrition/shared": "workspace:*",
    "zod": "^3.25.0"
  }
}
